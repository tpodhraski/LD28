package prototype
{
    import starling.display.Image;
    import starling.textures.Texture;

    public class FloorTile extends Image
    {
        public function FloorTile(texture:Texture)
        {
            super(texture);
        }
    }
}
